# Haregi(ハレギ)仕様書

「天気予報(気温)を見ながら、日ごとの服装コーディネートを記録・管理する」Web アプリ **Haregi** の要件仕様。**何を作るか**を定義する。技術的な実現方法(スタック・構成・データモデル・API)は [architecture.md](./architecture.md) を参照。

- 作成日: 2026-07-19([rebuildspec.md](./rebuildspec.md) からの分割。検討経緯はそちらを参照)
- 更新: 2026-07-25(architecture.md との整合性チェックに基づき、気温スナップショットの基準地域・写真の制限/サムネイル方針・検証の実施場所を明確化)
- 更新: 2026-08-01(§1.1 にターゲットユーザーを追加。人物像の詳細は [persona.md](./persona.md) に分離)
- ステータス: 確定(実装は新リポジトリで行う)

---

## 1. アプリ概要

**Haregi(ハレギ)** は、旧称 FabuForecast のリビルドにあたり改名した。名前は「**晴れ**(天気)+**着**(服)」のダブルミーニングで、天気と服をつなぐというアプリの本質を表す。リポジトリ名・パッケージスコープは `haregi` を用いる(例: `@haregi/schema`)。

ユーザーは居住地域(気象庁の府県予報区)を登録してアカウントを作成し、その地域の週間気温予報(最高/最低気温)を確認しながら、日付ごとに「アウター・トップス・ボトムス」を記録する。**過去の記録を気温と紐付けて振り返れる**ことが、単なる天気アプリとの差別化ポイント。

> プロジェクト名の正式決定前に、ドメイン・ストア・商標(J-PlatPat)の被り確認を推奨。

### 1.1 ターゲットユーザー

想定する中心ユーザーは **「週の初めに1週間分の服を計画したい、服好きの通勤者」**(20代後半〜30代・毎日電車通勤・気温の読み違いによる服装の失敗が中心課題)。人物像の詳細と、そこから導かれる設計方針は [persona.md](./persona.md) を参照。機能の優先順位・UI の判断はこの人物を基準に下す。

### 現行アプリの実装状況(移行元)

| 機能 | 状態 |
| --- | --- |
| 会員登録(ユーザーID/メール/都道府県/パスワード) | 実装済み |
| ログイン(NextAuth Credentials + JWT) | 実装済み |
| 週間気温予報の取得(Open-Meteo) | 実装済み(緯度経度がハードコード) |
| コーディネート入力 UI | 実装済み(保存 API が未実装) |
| コーディネート保存・閲覧・編集 | 未実装 |
| ログイン状態によるナビ出し分け | 未実装 |
| 都道府県マスタ | 3件のみ(東京/神奈川/広島) |

---

## 2. 機能要件

優先度: **Must**(初回リリース必須)/ **Should**(早期に追加)/ **Could**(将来候補)

### 2.1 認証・アカウント

| 機能 | 優先度 | 備考 |
| --- | --- | --- |
| 会員登録(メール+パスワード+ユーザー名+登録地域) | Must | |
| ログイン / ログアウト | Must | ログイン ID はメールアドレス |
| セッション管理・保護ページ | Must | 未ログインは `/login` へリダイレクト |
| アカウント設定(登録地域・ユーザー名・パスワード変更) | Should | |
| パスワードリセット(メール送信) | Could | メールプロバイダ(Resend 等)契約が前提 |
| メールアドレス検証 | Could | 同上 |
| アカウント削除 | Could | |

### 2.2 天気予報

| 機能 | 優先度 | 備考 |
| --- | --- | --- |
| 登録地域の週間予報(最高/最低気温) | Must | 気象庁の予報データを利用(現行のハードコードを解消) |
| 地域マスタ(気象庁 府県予報区) | Must | 現行3件 → **全58区分**(北海道7・沖縄4・奄美含む)。検証済みマスタデータを生成済み([architecture.md §6](./architecture.md) 参照) |
| 表示地域の切替(一覧から選択) | Must | 予報画面のセレクタで任意の地域の予報を閲覧できる。登録地域はデフォルト表示地域として扱う。**切替中に保存したコーデには、その時点で表示していた地域の気温が記録される**(§2.3) |
| 予報取得失敗時の表示 | Must | 予報部にエラーメッセージ+再試行ボタン。**予報が取れなくてもコーデ入力・保存は継続できる** |
| 出典表記(「出典: 気象庁ホームページ」) | Must | 政府標準利用規約の要件。予報表示部またはフッターに常時表示 |
| 天気アイコン(晴れ/曇り/雨) | Should | 気象庁の天気コードをアイコンにマッピング |
| 降水確率 | Should | 同じレスポンスに含まれるため追加リクエスト不要 |

### 2.3 コーディネート(中核機能)

| 機能 | 優先度 | 備考 |
| --- | --- | --- |
| 日付ごとのコーデ登録(アウター/トップス/ボトムス) | Must | ユーザー×日付で一意(upsert)。**記録時点の予報気温スナップショットを自動保存**する(§2.5 の布石。ユーザー操作は不要)。基準は**画面で表示中の地域**の予報とし、画面に出ていた気温と記録が一致することを保証する(予報が取れない場合は気温なしで保存。[architecture.md 決定事項 #29](./architecture.md) 参照) |
| 保存済みコーデの表示・編集 | Must | 予報画面に既存データを反映 |
| コーデの削除 | Should | 写真がある場合はストレージのオブジェクトも削除 |
| コーデ写真のアップロード(1日1枚) | Should | 5MB 程度・jpeg/png/webp に制限(サーバー側で強制できるかはストレージ契約先の presigned POST 対応に依存。[architecture.md §8](./architecture.md) 参照) |
| 過去コーデの履歴一覧 | Should | 記録アプリとしての価値。当日の気温・写真も併記(**サムネイルは生成せず原寸を縮小表示**。リサイズは Could) |
| 「似た気温の日に何を着たか」参照 | Could | 差別化機能。予報気温 ±2℃ の過去記録を提示 |
| AI コーディネート提案(写真 × トレンド × 天気) | Could(将来構想) | §2.5 参照 |
| メモ・小物など項目追加 | Could | |

### 2.4 共通 UI

| 機能 | 優先度 | 備考 |
| --- | --- | --- |
| ランディングページ | Must | 現行のヒーローを刷新(仮テキスト排除) |
| ログイン状態によるナビ出し分け | Must | 現行 TODO の解消 |
| トースト通知 | Must | |
| レスポンシブ対応 | Must | モバイルはドロワーナビ |
| ダークモード | Could | |

### 2.5 将来構想: AI コーディネート提案(Could)

保存済みのコーデ写真・過去の着用記録・ファッショントレンド情報・週間予報を組み合わせ、「明日はこの服装がおすすめ」を生成する機能。初回リリースには含めないが、設計段階から布石を打っておく(技術的な前方互換の担保は [architecture.md §5](./architecture.md) 参照)。

**実現方式(想定)**

1. 対象日の予報(気温・天気)を取得
2. 似た気温(±2℃程度)の過去コーデ(写真 + テキスト)を抽出
3. トレンド情報を取得(下記課題参照)
4. 1〜3 をマルチモーダル LLM API(Claude API 等)に渡し、手持ちの服を踏まえた提案文を生成
5. 生成結果は日付×地域単位でキャッシュしコストを抑制

**課題・リスク(実装時に要調査)**

- **トレンド情報の入手経路が最大の課題**。公開のファッショントレンド API はほぼ存在しないため、候補は (a) Web 検索 API 経由でファッションメディアを要約 (b) 季節・気温帯ごとの静的なトレンド辞書を自前管理 (c) LLM の一般知識に季節情報を添えて委ねる、のいずれか。コストと鮮度のバランスで選定する
- **LLM API はスタック初の従量課金要素**。ユーザーあたりの生成回数制限とキャッシュを必須とする
- 気象庁の予報値は改変せずそのまま提案の入力に使う(独自の気象予測を生成しないため、気象業務法上の予報業務許可は不要のまま)

---

## 3. 画面一覧

| パス | 画面 | 認証 | 優先度 |
| --- | --- | --- | --- |
| `/` | ランディング | 不要 | Must |
| `/signup` | 会員登録 | 不要 | Must |
| `/login` | ログイン | 不要 | Must |
| `/forecast` | 週間予報+コーデ入力(メイン画面)。地域切替セレクタ付き | 必要 | Must |
| `/history` | 過去コーデ履歴 | 必要 | Should |
| `/settings` | アカウント設定 | 必要 | Should |

> 現行の `/coordination/create` は `/forecast` に統合する(予報閲覧と入力を1画面に)。

---

## 4. 入力バリデーション(現行仕様を踏襲)

| 項目 | ルール |
| --- | --- |
| ユーザー名(表示名) | 必須 / 8文字以上 / 英数字のみ。**一意制約は付けない**(ログイン ID はメールアドレス。現行の「ユーザーID」から表示名へ位置づけを変更) |
| メールアドレス | 必須 / メール形式 |
| 登録地域 | 必須 / 地域マスタ(気象庁 府県予報区)に存在するコード |
| パスワード | 必須 / 8〜20文字 / 小文字英字と数字を含む |
| パスワード確認 | パスワードと一致 |
| コーデ各項目 | 任意 / 最大50文字程度 |

- エラーメッセージは日本語(現行文言を流用)
- バリデーションはフロント(UX 用)とサーバー(防御用)の両方で行う。実装方式は、会員登録・アカウント設定・パスワード変更が [architecture.md §7](./architecture.md)(Better Auth の `hooks.before`)、コーデ各項目が [architecture.md §5](./architecture.md)(`@hono/zod-validator`)

---

## 5. 表示・挙動の仕様

- **日付表示**: `YYYY年M月D日`、気温は `℃` 表示(現行仕様を踏襲)
- **日付の基準**: すべて日本時間(JST)。深夜0時前後でも「今日」の判定がぶれないこと
- **障害時挙動**: 気象庁からの予報取得に失敗した場合、予報部にエラーメッセージと再試行ボタンを表示する(取得済みの古いデータがあればそれを表示)。コーデの入力・保存は予報なしでも継続できる
- **出典表記**: 「出典: 気象庁ホームページ」を予報表示部またはフッターに常時表示する

---

## 6. 見送った事項(明示的な非スコープ)

- Supabase 等の BaaS 利用
- Storybook / Husky + lint-staged / GitHub Actions CI
- メール送信を伴う機能(パスワードリセット・メール検証)
- **パスワード忘失時の救済手段**(リセット機能・管理スクリプトとも初回は用意しない。必要になったら DB / 認証ライブラリの API を直接操作して対応する、と割り切る)
- ソーシャルログイン(後付け可能)
- E2E テスト
- デプロイ先の決定(明示的保留。[architecture.md §2](./architecture.md) の決定事項 #23 参照)

---

## 付録: 現行リポジトリから引き継ぐ資産

- 日本語バリデーションメッセージと入力ルール(§4)
- 「一覧から地域を選択する」UI の考え方(現行の都道府県セレクトを気象庁 府県予報区の一覧セレクタに発展させる)
- 気温整形(`YYYY年M月D日` / `℃` 表示)の仕様
- ヒーロー画像(`public/clouds.jpg`)は任意で流用
